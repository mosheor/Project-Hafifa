FROM registry.access.redhat.com/rhel7.5

#FROM registry.access.redhat.com/rhscl/python-36-rhel7
#FROM fedora:28
COPY . /
USER root
WORKDIR /
#RUN subscription-manager register --username=DoctorBD --password=doctore22 && \
#subscription-manager attach --auto
RUN yum install -y python3-devel cmake gcc gcc-c++ make zlib-devel openssl openssl-devel libXext libSM libXrender
RUN pip3.6 install /pipPackages/*
EXPOSE 5000
#CMD ["echo", "hi there"]
CMD ["python3", "/flask_server.py"]